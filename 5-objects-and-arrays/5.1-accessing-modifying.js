// This file provides examples of accessing and modifying properties of objects in JavaScript.

// Accessing properties using dot notation.
const person = {
  name: "John Doe",
  age: 30,
};

console.log(person.name);
console.log(person["age"]);

// Modifying properties.
person.age = 31;
console.log(person.age);